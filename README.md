# homebrew-tap

Install
-------

```sh
brew tap ryochin/tap
```

```sh
brew install easyedit
brew install gb
brew install glint
```

Cleanup
-------

```sh
rm -rf /opt/homebrew//Library/Taps/ryochin/homebrew-tap
```
